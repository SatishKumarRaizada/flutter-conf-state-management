enum ThemeMode {
  // Use either the light or dark theme based on what the user has selected in
  // the system settings.
  system,

  // Regardless of system settings, always use the light mode.
  light,

  // Regardless of the system settings, always use the dark mode.
  dark,
}
